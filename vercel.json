{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.js",  # Punto de entrada de tu backend
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/.*",           # Todas las rutas bajo /api
      "dest": "backend/server.js"  # Redirige al backend
    }
  ]
}